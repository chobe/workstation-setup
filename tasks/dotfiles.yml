- name: Installing stow
  become: true
  apt: name=stow
  tags:
    - install
    - dotfiles
    - stow

- name: Stow dotfiles
  shell: cd $HOME/dotfiles/main && ./ubuntu
  tags:
    - install
    - dotfiles
    - stow
